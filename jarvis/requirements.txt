# Core contracts and validation
pydantic>=2.0

# HTTP: model providers and web environment
requests>=2.31

# Web environment: page parsing and search
beautifulsoup4>=4.12
ddgs>=9.0

# CLI rendering (route-trace panel)
rich>=13.0

# Windows GUI automation via the UI Automation accessibility tree
pywinauto>=0.6.9

# System state: CPU, RAM, disk, process table
psutil>=5.9

# Tests
pytest>=8.0

# Local web control interface
fastapi>=0.110
uvicorn[standard]>=0.29

# Voice: Gemini Live bidirectional audio, native mic/speaker transport,
# browser-bridge PCM handling
websockets>=12.0
sounddevice>=0.4.6
numpy>=1.26

# Windows GUI: SAPI text-to-speech COM binding (fallback half-duplex voice)
pywin32>=306
